package com.example.guiainteractiva.ui.theme

import androidx.compose.ui.graphics.Color

// PALETA DE COLORES
// Aquí se definen todos los colores que se usan en la app.
// Mantenerlos juntos hace fácil ajustar el diseño más adelante.



// Tema Claro
val LightGreenBg = Color(0xFFD0E7D2)   // Fondo principal muy claro
val PastelGreenBg = Color(0xFF6AAE77)  // Verde medio para etiquetas y elementos destacados
val GreenButton = Color(0xFF75A47F)    // Botones principales

val GrayField = Color(0xFFEAEAEA)      // Campos de texto en modo claro
// Se diferencia el color del botón para que no sea idéntico al fondo del campo de texto.
val LightBeigeButton = Color(0xFFF2F2F2) // Botón secundario claro


// Tema Oscuro
val DarkGreenBg = Color(0xFF223026)        // Fondo principal oscuro
val DarkPastelGreenBg = Color(0xFF39513F)  // Verde oscuro para etiquetas
val DarkGrayField = Color(0xFF2C2C2C)      // Campos de texto oscuros
val DarkBeigeButton = Color(0xFF424242)    // Botón secundario oscuro
val DarkFormBg = Color(0xFF1C1C1E)         // Fondo del formulario en oscuro

// Colores de Acción
val ConfirmGreen = Color(0xFF4CAF50) // Confirmar / guardar
val ErrorRed = Color(0xFFF44336)     // Eliminar / cancelar cambios

// Colores de Texto
val WhiteText = Color.White
val BlackText = Color.Black
